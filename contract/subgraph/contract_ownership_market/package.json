{
  "name": "contract_ownership_market",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ contract_ownership_market",
    "create-local": "graph create --node http://localhost:8020/ contract_ownership_market",
    "remove-local": "graph remove --node http://localhost:8020/ contract_ownership_market",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 contract_ownership_market",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.91.1",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
